<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.katiechen.foodthree">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainIngreInputActivity"
            android:label="@string/title_activity_main2" />
        <activity android:name=".MainCuisineActivity" />
        <activity android:name=".MainRankActivity" />
        <activity android:name=".MainHowToUseActivity" />
        <activity android:name=".MainCategoryActivity" />
        <activity android:name=".MainIngreOutputActivity" />
        <activity android:name=".MainRecipeRankActivity" />

        <activity android:name=".Main8Activity"></activity>

        <activity android:name=".MainFullRecipeActivity" />
        <activity android:name=".MainRandomRecipeActivity"></activity>

    </application>

</manifest>